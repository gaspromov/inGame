export class User {
    accessToken ? : string
    email: string;
    password: string;
    name?: string;
    number?: string;
    typeUser?: string;
    telegram?: string;
    social?: any = [];
    vk?: string
}
